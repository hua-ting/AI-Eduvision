# 学习推荐系统 - 快速启动指南

## 📁 项目结构

```
biyesheji/
├── database/              # 数据库脚本
│   ├── init.sql          # 初始化建表SQL
│   └── test_data.sql     # 测试数据
├── backend/              # Spring Boot后端
│   ├── src/
│   ├── pom.xml
│   └── application.yml
├── frontend/             # Vue3前端
│   ├── src/
│   ├── package.json
│   └── vite.config.js
├── ai-service/           # Python AI服务
│   ├── main.py
│   ├── requirements.txt
│   └── README.md
└── 项目整体设计方案.md
```

## 🚀 环境要求

### 必需环境
- **JDK**: 1.8+
- **Maven**: 3.6+
- **Node.js**: 16+
- **Python**: 3.9+
- **MySQL**: 8.0+
- **Redis**: 7.x

## 📝 启动步骤

### 第一步: 数据库初始化

1. 启动MySQL服务
2. 执行建表脚本:
```bash
mysql -u root -p < database/init.sql
```

3. (可选)导入测试数据:
```bash
mysql -u root -p < database/test_data.sql
```

### 第二步: 启动Redis

```bash
# Windows
redis-server

# Linux/Mac
redis-server /path/to/redis.conf
```

### 第三步: 启动后端服务

```bash
cd backend

# 使用Maven启动
mvn spring-boot:run

# 或使用IDEA直接运行RecommendApplication
```

后端服务: http://localhost:8080/api
API文档: http://localhost:8080/api/doc.html

### 第四步: 启动AI服务

```bash
cd ai-service

# 安装依赖(首次)
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 启动服务
python main.py
```

AI服务: http://localhost:5000
API文档: http://localhost:5000/docs

### 第五步: 启动前端

```bash
cd frontend

# 安装依赖(首次)
npm install

# 启动开发服务器
npm run dev
```

前端地址: http://localhost:5173

## 📊 测试账号

### 管理员
- 用户名: `admin`
- 密码: `admin123`

### 学生账号
- 用户名: `student01` ~ `student20`
- 密码: `123456`

## ⚙️ 配置修改

### 数据库配置
修改 `backend/src/main/resources/application.yml`:
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learning_recommend
    username: root
    password: 你的密码
```

### Redis配置
修改 `backend/src/main/resources/application.yml`:
```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: 你的密码(如果有)
```

## 🔍 验证服务

### 1. 检查后端
访问: http://localhost:8080/api/doc.html
应该能看到Knife4j接口文档

### 2. 检查AI服务
访问: http://localhost:5000/health
返回: `{"status":"healthy",...}`

### 3. 检查前端
访问: http://localhost:5173
应该能看到登录页面

## 🐛 常见问题

### 问题1: 端口被占用
**解决**: 修改配置文件中的端口号
- 后端: application.yml 中的 `server.port`
- 前端: vite.config.js 中的 `server.port`
- AI服务: main.py 中的 `uvicorn.run(..., port=5000)`

### 问题2: 数据库连接失败
**检查**:
1. MySQL服务是否启动
2. 数据库名称是否正确
3. 用户名密码是否正确

### 问题3: Maven依赖下载慢
**解决**: 配置国内镜像源
在 `pom.xml` 同级目录创建 `settings.xml`:
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### 问题4: npm安装依赖慢
**解决**: 使用国内镜像
```bash
npm config set registry https://registry.npmmirror.com
```

## 📦 下一步开发

当前已完成:
- ✅ 数据库表结构设计
- ✅ 后端项目框架搭建
- ✅ 前端项目框架搭建
- ✅ AI服务基础框架

待开发:
- ⏳ 用户登录注册功能
- ⏳ 资料管理模块
- ⏳ 推荐算法实现
- ⏳ 前端页面开发

## 💡 开发建议

1. **先跑通基础功能**: 登录→资料列表→资料详情
2. **再实现推荐**: 简单协同过滤→混合推荐
3. **最后优化**: UI美化→性能优化→测试数据

## 📞 技术支持

遇到问题请参考:
- [项目整体设计方案.md](./项目整体设计方案.md)
- Spring Boot文档: https://spring.io/projects/spring-boot
- Vue3文档: https://vuejs.org/
- Naive UI文档: https://www.naiveui.com/
