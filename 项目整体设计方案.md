# 基于Transformer的个性化学习推荐系统 - 整体设计方案

> **项目类型**: 毕业设计  
> **架构**: 前后端分离  
> **前端**: Vue 3 + Naive UI  
> **后端**: Spring Boot + MySQL + Redis  
> **AI模型**: Transformer(BART/T5) - 独立Python服务

---

## 一、系统架构设计

### 1.1 总体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层 (Browser)                      │
│                     Vue 3 + Naive UI                        │
└────────────────┬────────────────────────────────────────────┘
                 │ HTTP/HTTPS (Axios)
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                      网关层 (可选)                           │
│                    Nginx (生产环境)                          │
└────────────────┬────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                   后端服务层 (Spring Boot)                   │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │用户服务  │资料服务  │推荐服务  │行为服务  │摘要服务  │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘  │
└────────┬─────────────────────────────────────┬──────────────┘
         │                                     │ RestTemplate
         │                                     ▼
         │                        ┌──────────────────────────┐
         │                        │   AI模型服务 (Python)    │
         │                        │   Flask/FastAPI          │
         │                        │   BART/T5 Transformer    │
         │                        └──────────────────────────┘
         ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
│  ┌──────────┬──────────┬──────────────┐                    │
│  │  MySQL   │  Redis   │  MinIO/OSS   │                    │
│  │(主数据库)│  (缓存)  │  (文件存储)  │                    │
│  └──────────┴──────────┴──────────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、技术栈详细说明

### 2.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.3+ | 核心框架 |
| Vite | 4.x | 构建工具 |
| Naive UI | 2.35+ | UI组件库(年轻化风格) |
| Pinia | 2.x | 状态管理 |
| Vue Router | 4.x | 路由管理 |
| Axios | 1.x | HTTP请求 |
| ECharts | 5.x | 数据可视化 |
| VueUse | 10.x | 组合式API工具 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 核心框架 |
| MyBatis-Plus | 3.5.5 | ORM框架 |
| MySQL | 8.0 | 关系型数据库 |
| Redis | 7.x | 缓存/会话 |
| JWT | - | 用户认证 |
| Lombok | 1.18.30 | 简化代码 |
| Hutool | 5.8.x | 工具类库 |
| Knife4j | 4.x | API文档 |

### 2.3 AI模型服务

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.9+ | 运行环境 |
| FastAPI | 0.104+ | Web框架(推荐) |
| Transformers | 4.35+ | Hugging Face库 |
| PyTorch | 2.0+ | 深度学习框架 |
| BART/T5 | - | 摘要生成模型 |

---

## 三、核心功能模块设计

### 3.1 用户端功能

#### 3.1.1 首页Dashboard
- **学习统计可视化**
  - 学习时长趋势图(折线图)
  - 资料分类占比(饼图)
  - 最近学习记录(时间轴)
- **快捷入口**: 资料广场、智能推荐、我的收藏

#### 3.1.2 资料广场
- **功能**:
  - 关键词搜索 + 分类筛选
  - 资料卡片展示(标题、摘要、标签、评分)
  - 排序: 最新/最热/评分
- **交互**:
  - 点击查看详情(含AI摘要)
  - 收藏/评分/下载

#### 3.1.3 智能推荐
- **推荐列表**:
  - 基于用户画像的个性化推荐
  - 推荐理由展示("因为你学过XXX")
  - 实时刷新按钮
- **多样性**:
  - 热门推荐
  - 相似资料推荐
  - 新资料探索

#### 3.1.4 资料详情页
- **内容展示**:
  - 基本信息(标题、作者、分类、标签)
  - **AI生成摘要**(核心亮点)
  - 原文预览/下载
  - 相关推荐
- **交互**:
  - 收藏/取消收藏
  - 星级评分
  - 学习记录(自动记录浏览时长)

#### 3.1.5 个人中心
- **我的信息**: 修改头像、昵称、学习偏好
- **我的收藏**: 收藏列表管理
- **学习记录**: 浏览历史、学习轨迹
- **学习偏好设置**: 兴趣标签选择(用于冷启动)

### 3.2 管理员端功能

#### 3.2.1 资料管理
- 资料上传(支持PDF/Word/TXT)
- 资料审核/编辑/删除
- 批量导入
- 标签管理

#### 3.2.2 用户管理
- 用户列表(搜索/冻结/删除)
- 用户画像查看
- 学习行为统计

#### 3.2.3 系统监控
- 推荐效果监控(点击率、收藏率)
- 模型服务状态
- 系统日志

---

## 四、数据库设计

### 4.1 核心表结构

#### 用户表 (t_user)
```sql
CREATE TABLE t_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) UNIQUE NOT NULL COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
    nickname VARCHAR(50) COMMENT '昵称',
    avatar VARCHAR(255) COMMENT '头像URL',
    email VARCHAR(100) COMMENT '邮箱',
    role TINYINT DEFAULT 0 COMMENT '角色:0学生 1管理员',
    status TINYINT DEFAULT 1 COMMENT '状态:0禁用 1启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username(username)
) COMMENT '用户表';
```

#### 用户画像表 (t_user_profile)
```sql
CREATE TABLE t_user_profile (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE NOT NULL COMMENT '用户ID',
    learning_level VARCHAR(20) COMMENT '学习水平:初级/中级/高级',
    interest_tags JSON COMMENT '兴趣标签数组',
    active_time JSON COMMENT '活跃时段统计',
    learning_duration INT DEFAULT 0 COMMENT '累计学习时长(分钟)',
    profile_vector TEXT COMMENT '用户画像向量(JSON)',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES t_user(id) ON DELETE CASCADE
) COMMENT '用户画像表';
```

#### 学习资料表 (t_material)
```sql
CREATE TABLE t_material (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL COMMENT '标题',
    category VARCHAR(50) NOT NULL COMMENT '分类:教材/课件/论文/视频',
    file_url VARCHAR(500) COMMENT '文件URL',
    file_type VARCHAR(20) COMMENT '文件类型:pdf/docx/txt',
    file_size BIGINT COMMENT '文件大小(字节)',
    description TEXT COMMENT '描述',
    tags JSON COMMENT '标签数组',
    view_count INT DEFAULT 0 COMMENT '浏览量',
    collect_count INT DEFAULT 0 COMMENT '收藏量',
    avg_rating DECIMAL(3,2) DEFAULT 0 COMMENT '平均评分',
    rating_count INT DEFAULT 0 COMMENT '评分人数',
    status TINYINT DEFAULT 1 COMMENT '状态:0下架 1上架',
    uploader_id BIGINT COMMENT '上传者ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_category(category),
    INDEX idx_status(status),
    INDEX idx_create_time(create_time)
) COMMENT '学习资料表';
```

#### 资料摘要表 (t_material_summary)
```sql
CREATE TABLE t_material_summary (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    material_id BIGINT UNIQUE NOT NULL COMMENT '资料ID',
    summary_text TEXT COMMENT 'AI生成摘要',
    summary_type VARCHAR(20) COMMENT '摘要类型:抽取式/生成式',
    key_points JSON COMMENT '关键要点数组',
    model_name VARCHAR(50) COMMENT '使用模型:BART/T5',
    generate_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (material_id) REFERENCES t_material(id) ON DELETE CASCADE
) COMMENT '资料摘要表';
```

#### 用户行为表 (t_user_behavior)
```sql
CREATE TABLE t_user_behavior (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    material_id BIGINT NOT NULL COMMENT '资料ID',
    behavior_type VARCHAR(20) NOT NULL COMMENT '行为类型:view/collect/download/rate',
    duration INT COMMENT '浏览时长(秒)',
    rating TINYINT COMMENT '评分1-5',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_material(user_id, material_id),
    INDEX idx_behavior_type(behavior_type),
    INDEX idx_create_time(create_time)
) COMMENT '用户行为表';
```

#### 推荐记录表 (t_recommendation_log)
```sql
CREATE TABLE t_recommendation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    material_id BIGINT NOT NULL COMMENT '资料ID',
    score DECIMAL(5,4) COMMENT '推荐分数',
    algorithm VARCHAR(50) COMMENT '算法类型:CF/CB/Hybrid',
    is_clicked TINYINT DEFAULT 0 COMMENT '是否点击',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time(user_id, create_time)
) COMMENT '推荐记录表';
```

#### 标签表 (t_tag)
```sql
CREATE TABLE t_tag (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) UNIQUE NOT NULL COMMENT '标签名',
    category VARCHAR(50) COMMENT '分类',
    use_count INT DEFAULT 0 COMMENT '使用次数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT '标签表';
```

---

## 五、推荐算法设计

### 5.1 协同过滤 (Collaborative Filtering)

#### 5.1.1 基于用户的协同过滤 (User-Based CF)
```
核心思想: 找到相似用户,推荐他们喜欢的资料

步骤:
1. 计算用户相似度 (余弦相似度/皮尔逊相关系数)
   - 基于行为矩阵: 用户-资料评分/浏览时长
2. 找出Top-K相似用户
3. 预测评分 = Σ(相似度 × 邻居评分) / Σ相似度
4. 推荐Top-N未浏览资料
```

**实现方式**: 
- 离线计算(定时任务每天凌晨执行)
- 结果存入Redis (key: `rec:user:{userId}`, TTL: 24h)

#### 5.1.2 基于物品的协同过滤 (Item-Based CF)
```
核心思想: 推荐与用户历史喜欢资料相似的资料

步骤:
1. 计算资料相似度矩阵
   - 基于共同评分用户
2. 根据用户历史,推荐相似资料
```

### 5.2 基于内容的推荐 (Content-Based)

```
核心思想: 基于资料内容特征匹配用户兴趣

步骤:
1. 资料特征提取:
   - TF-IDF向量 (标题+描述+标签)
   - 分类/标签one-hot编码
2. 用户画像特征:
   - 历史浏览资料特征聚合
   - 兴趣标签权重
3. 余弦相似度计算
4. Top-N推荐
```

**实现**: 
- 使用HanLP/jieba分词
- scikit-learn的TfidfVectorizer

### 5.3 混合推荐策略

```
最终推荐分数 = α×CF_score + β×CB_score + γ×热度_score

参数建议:
- α = 0.5 (协同过滤权重)
- β = 0.3 (内容权重)
- γ = 0.2 (热度权重)

冷启动处理:
- 新用户: 热门推荐 + 兴趣标签匹配
- 新资料: 内容推荐 + 分类热门
```

### 5.4 Transformer语义增强 (可选进阶)

```
利用已训练的Transformer模型提取语义特征

步骤:
1. 调用模型API获取资料embedding
2. 计算用户历史资料embedding均值
3. 语义相似度 = cosine(user_emb, material_emb)
4. 融入混合推荐
```

---

## 六、AI摘要服务设计

### 6.1 Python服务架构

**推荐框架**: FastAPI (高性能+异步支持)

**项目结构**:
```
ai_service/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI入口
│   ├── models/
│   │   ├── summarizer.py    # 摘要模型封装
│   │   └── model_config.py  # 模型配置
│   ├── api/
│   │   └── routes.py        # API路由
│   └── utils/
│       └── text_processor.py # 文本预处理
├── models/                  # 模型文件存放
│   ├── bart-base-chinese/
│   └── t5-pegasus-small/
├── requirements.txt
└── config.yaml
```

### 6.2 API接口设计

#### 6.2.1 生成摘要接口
```
POST /api/summarize
Content-Type: application/json

Request:
{
    "text": "原文内容...",
    "max_length": 150,        // 摘要最大长度
    "min_length": 50,         // 摘要最小长度
    "summary_type": "abstract" // abstract/extractive
}

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "summary": "生成的摘要文本...",
        "key_points": ["要点1", "要点2"],
        "model": "bart-base-chinese",
        "duration": 1.23  // 耗时(秒)
    }
}
```

#### 6.2.2 健康检查接口
```
GET /health

Response:
{
    "status": "healthy",
    "model_loaded": true,
    "gpu_available": false
}
```

### 6.3 模型选择

| 模型 | 优势 | 劣势 | 推荐场景 |
|------|------|------|----------|
| **BART** | 生成式,质量高 | 速度较慢,需GPU | 毕业设计演示(效果好) |
| **T5-Pegasus** | 中文优化,轻量 | 需中文数据微调 | 实际部署(平衡) |
| **TextRank** | 无需训练,快速 | 抽取式,质量一般 | 冷启动/备用方案 |

**推荐**: 
- 开发阶段: T5-Pegasus-small (速度快,资源少)
- 答辩演示: BART-base-chinese (效果佳)

### 6.4 SpringBoot调用示例

```java
@Service
public class SummaryService {
    private static final String AI_SERVICE_URL = "http://localhost:5000/api/summarize";
    
    @Autowired
    private RestTemplate restTemplate;
    
    public String generateSummary(String text) {
        Map<String, Object> request = new HashMap<>();
        request.put("text", text);
        request.put("max_length", 150);
        
        ResponseEntity<Map> response = restTemplate.postForEntity(
            AI_SERVICE_URL, request, Map.class
        );
        
        return (String) response.getBody().get("summary");
    }
}
```

---

## 七、前端页面设计

### 7.1 路由设计

```javascript
const routes = [
  {
    path: '/login',
    component: () => import('@/views/Login.vue')
  },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      // 学生端
      {
        path: 'dashboard',
        component: () => import('@/views/student/Dashboard.vue'),
        meta: { title: '首页', icon: 'dashboard' }
      },
      {
        path: 'materials',
        component: () => import('@/views/student/MaterialSquare.vue'),
        meta: { title: '资料广场', icon: 'book' }
      },
      {
        path: 'material/:id',
        component: () => import('@/views/student/MaterialDetail.vue'),
        meta: { title: '资料详情' }
      },
      {
        path: 'recommend',
        component: () => import('@/views/student/Recommend.vue'),
        meta: { title: '智能推荐', icon: 'star' }
      },
      {
        path: 'collection',
        component: () => import('@/views/student/MyCollection.vue'),
        meta: { title: '我的收藏', icon: 'heart' }
      },
      {
        path: 'profile',
        component: () => import('@/views/student/Profile.vue'),
        meta: { title: '个人中心', icon: 'user' }
      },
      // 管理员端
      {
        path: 'admin/materials',
        component: () => import('@/views/admin/MaterialManage.vue'),
        meta: { title: '资料管理', role: 'admin' }
      },
      {
        path: 'admin/users',
        component: () => import('@/views/admin/UserManage.vue'),
        meta: { title: '用户管理', role: 'admin' }
      }
    ]
  }
]
```

### 7.2 核心组件设计

#### 7.2.1 资料卡片组件 (MaterialCard.vue)
```vue
<template>
  <n-card hoverable>
    <template #cover>
      <img :src="material.cover || defaultCover" />
    </template>
    <n-space vertical>
      <n-text strong>{{ material.title }}</n-text>
      <n-ellipsis :line-clamp="2">
        {{ material.summary }}
      </n-ellipsis>
      <n-space>
        <n-tag v-for="tag in material.tags" :key="tag" size="small">
          {{ tag }}
        </n-tag>
      </n-space>
      <n-space justify="space-between">
        <n-rate :value="material.rating" readonly size="small" />
        <n-space>
          <n-button text @click="collect">
            <n-icon :component="HeartOutline" />
          </n-button>
          <n-text depth="3">{{ material.collectCount }}</n-text>
        </n-space>
      </n-space>
    </n-space>
  </n-card>
</template>
```

#### 7.2.2 推荐列表组件 (RecommendList.vue)
```vue
<template>
  <n-list>
    <n-list-item v-for="item in recommendations" :key="item.id">
      <n-thing>
        <template #avatar>
          <n-avatar :src="item.cover" />
        </template>
        <template #header>
          <n-text>{{ item.title }}</n-text>
        </template>
        <template #description>
          <n-tag size="tiny" type="info">
            {{ item.reason }}
          </n-tag>
        </template>
        {{ item.summary }}
        <template #action>
          <n-space>
            <n-button size="small" @click="view(item)">查看</n-button>
            <n-button size="small" secondary @click="notInterested(item)">
              不感兴趣
            </n-button>
          </n-space>
        </template>
      </n-thing>
    </n-list-item>
  </n-list>
</template>
```

### 7.3 数据可视化设计

#### Dashboard图表配置
```javascript
// 学习时长趋势图
const learningTrendOption = {
  xAxis: { type: 'category', data: ['周一', '周二', ...] },
  yAxis: { type: 'value', name: '分钟' },
  series: [{
    data: [30, 45, 60, ...],
    type: 'line',
    smooth: true,
    areaStyle: {}
  }]
}

// 资料分类占比
const categoryPieOption = {
  series: [{
    type: 'pie',
    radius: '60%',
    data: [
      { value: 20, name: '教材' },
      { value: 15, name: '课件' },
      { value: 10, name: '论文' }
    ]
  }]
}
```

---

## 八、后端接口设计

### 8.1 用户模块 API

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 注册 | POST | /api/user/register | 用户注册 |
| 登录 | POST | /api/user/login | 登录获取Token |
| 获取信息 | GET | /api/user/info | 获取当前用户信息 |
| 更新信息 | PUT | /api/user/info | 更新用户信息 |
| 设置兴趣 | PUT | /api/user/interests | 设置兴趣标签 |

### 8.2 资料模块 API

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 资料列表 | GET | /api/material/list | 分页查询 |
| 资料详情 | GET | /api/material/{id} | 获取详情+摘要 |
| 资料搜索 | GET | /api/material/search | 关键词搜索 |
| 上传资料 | POST | /api/material/upload | 管理员上传 |
| 删除资料 | DELETE | /api/material/{id} | 管理员删除 |

### 8.3 推荐模块 API

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取推荐 | GET | /api/recommend/list | 个性化推荐列表 |
| 刷新推荐 | POST | /api/recommend/refresh | 实时刷新推荐 |
| 热门推荐 | GET | /api/recommend/hot | 热门资料 |

### 8.4 行为模块 API

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 记录浏览 | POST | /api/behavior/view | 记录浏览行为 |
| 收藏资料 | POST | /api/behavior/collect | 收藏/取消收藏 |
| 评分资料 | POST | /api/behavior/rate | 提交评分 |
| 学习记录 | GET | /api/behavior/history | 获取学习历史 |

---

## 九、开发计划

### 第一阶段: 环境搭建 + 基础框架 (3-4天)

**任务清单**:
- [ ] 创建数据库, 执行建表SQL
- [ ] 初始化Spring Boot项目
  - [ ] 配置MyBatis-Plus
  - [ ] 配置Redis
  - [ ] 集成JWT认证
- [ ] 初始化Vue3项目
  - [ ] 安装Naive UI
  - [ ] 配置路由
  - [ ] 封装Axios
- [ ] 搭建Python AI服务
  - [ ] FastAPI项目初始化
  - [ ] 下载预训练模型
  - [ ] 测试摘要接口

### 第二阶段: 核心功能开发 (7-10天)

**后端开发**:
- [ ] 用户模块 (注册/登录/JWT)
- [ ] 资料模块 (CRUD/搜索)
- [ ] 摘要服务 (调用Python API)
- [ ] 行为记录模块

**前端开发**:
- [ ] 登录/注册页面
- [ ] 资料广场页面
- [ ] 资料详情页面
- [ ] Dashboard页面(基础版)

### 第三阶段: 推荐算法实现 (5-7天)

- [ ] 协同过滤算法实现
  - [ ] 用户相似度计算
  - [ ] 推荐生成逻辑
- [ ] 基于内容推荐
  - [ ] TF-IDF特征提取
  - [ ] 相似度计算
- [ ] 用户画像构建
  - [ ] 兴趣标签统计
  - [ ] 学习水平评估
- [ ] 推荐接口开发
- [ ] 推荐页面开发

### 第四阶段: 管理员功能 + 优化 (3-5天)

- [ ] 管理员资料管理
- [ ] 用户管理
- [ ] 数据可视化完善
- [ ] 前端UI美化
- [ ] 性能优化(Redis缓存)
- [ ] 接口文档生成(Knife4j)

### 第五阶段: 测试 + 部署准备 (2-3天)

- [ ] 功能测试
- [ ] 推荐效果测试
- [ ] 演示数据准备
- [ ] 部署文档编写

---

## 十、Credit使用策略 (2000预算)

### 关键使用场景 (优先级排序)

1. **推荐算法调试** (~600 credit)
   - 协同过滤逻辑bug修复
   - 推荐效果优化建议

2. **前端UI美化** (~500 credit)
   - 关键页面布局优化
   - 响应式适配问题

3. **后端复杂逻辑** (~400 credit)
   - 用户画像计算
   - 摘要服务集成

4. **数据库优化** (~200 credit)
   - 查询性能优化
   - 索引设计建议

5. **预留调试** (~300 credit)
   - 答辩前bug修复
   - 紧急问题解决

### 免费模型处理场景

- 基础CRUD代码生成
- 配置文件编写
- SQL语句生成
- 简单页面模板
- 工具类代码

---

## 十一、技术难点与解决方案

### 11.1 冷启动问题

**问题**: 新用户无历史数据,推荐困难

**解决方案**:
1. 注册时让用户选择兴趣标签(3-5个)
2. 基于标签推荐热门资料
3. 前10次浏览快速建立初步画像
4. 逐步过渡到个性化推荐

### 11.2 推荐实时性

**问题**: 用户行为变化后推荐未及时更新

**解决方案**:
1. 离线推荐(每日凌晨批量计算) → Redis缓存
2. 实时推荐(用户点击"刷新") → 基于最新行为计算
3. 混合策略: 80%离线 + 20%实时

### 11.3 AI模型性能

**问题**: 摘要生成速度慢

**解决方案**:
1. 异步处理: 上传资料后后台生成摘要
2. 结果缓存: 生成后存数据库
3. 模型量化: 使用轻量级模型
4. 可选降级: 超时时返回TextRank摘要

### 11.4 数据稀疏性

**问题**: 评分数据少,协同过滤效果差

**解决方案**:
1. 隐式反馈: 浏览时长>30s = 喜欢
2. 多行为融合: 浏览+收藏+评分
3. 内容推荐补充: CB算法不依赖评分

---

## 十二、演示数据准备

### 12.1 用户数据

- **学生用户**: 20-30个
  - 不同兴趣标签
  - 不同学习水平
  - 模拟真实行为(浏览/收藏/评分)

- **管理员**: 1-2个

### 12.2 资料数据

- **教材**: 15-20份
- **课件**: 20-30份
- **论文**: 10-15份

**内容覆盖**:
- 计算机类: 算法/数据库/AI
- 数学类: 高数/线代/概率论
- 其他: 英语/物理等

### 12.3 行为数据

- 为每个用户生成50-100条行为记录
- 确保推荐算法有足够数据
- 制造一些相似用户群体

---

## 十三、答辩要点

### 13.1 技术亮点

1. **Transformer应用**: BART/T5自动摘要生成
2. **混合推荐**: CF + CB + 热度的融合策略
3. **用户画像**: 基于多维行为数据构建
4. **前后端分离**: 现代化开发架构
5. **微服务思想**: AI模型独立部署

### 13.2 创新点

1. 教育领域的Transformer实践
2. 摘要与推荐的结合(摘要辅助推荐决策)
3. 动态画像更新机制
4. 多样性推荐(避免信息茧房)

### 13.3 可展示功能

1. **摘要生成演示**: 上传资料 → 自动生成摘要
2. **推荐效果**: 不同用户看到不同推荐
3. **数据可视化**: Dashboard统计图表
4. **实时更新**: 行为后推荐变化

### 13.4 可能提问及应对

**Q: 为什么选择BART/T5?**
A: 这两个模型在摘要任务上表现优异,BART擅长生成式摘要,T5支持多任务微调,且Hugging Face提供预训练模型,便于快速集成。

**Q: 推荐算法如何评估?**
A: 使用准确率、召回率、覆盖率指标,通过A/B测试对比不同算法,同时记录用户点击率和收藏率作为在线指标。

**Q: 冷启动如何解决?**
A: 新用户通过兴趣标签选择建立初始画像,推荐热门+标签匹配资料;新资料基于内容特征推荐,不依赖历史数据。

**Q: 性能优化措施?**
A: 推荐结果Redis缓存(24h),数据库索引优化,AI模型异步调用,前端懒加载和分页。

---

## 十四、项目文件结构

### 14.1 后端项目结构
```
learning-recommend-backend/
├── src/main/java/com/example/recommend/
│   ├── RecommendApplication.java
│   ├── config/
│   │   ├── RedisConfig.java
│   │   ├── MyBatisPlusConfig.java
│   │   └── SecurityConfig.java
│   ├── controller/
│   │   ├── UserController.java
│   │   ├── MaterialController.java
│   │   ├── RecommendController.java
│   │   └── BehaviorController.java
│   ├── service/
│   │   ├── UserService.java
│   │   ├── MaterialService.java
│   │   ├── RecommendService.java
│   │   ├── SummaryService.java
│   │   └── UserProfileService.java
│   ├── mapper/
│   │   ├── UserMapper.java
│   │   ├── MaterialMapper.java
│   │   └── BehaviorMapper.java
│   ├── entity/
│   │   ├── User.java
│   │   ├── Material.java
│   │   ├── UserProfile.java
│   │   └── UserBehavior.java
│   ├── dto/
│   │   ├── LoginDTO.java
│   │   ├── MaterialDTO.java
│   │   └── RecommendDTO.java
│   ├── utils/
│   │   ├── JwtUtil.java
│   │   ├── RedisUtil.java
│   │   └── RecommendAlgorithm.java
│   └── common/
│       ├── Result.java
│       └── Constants.java
├── src/main/resources/
│   ├── application.yml
│   ├── application-dev.yml
│   └── mapper/
└── pom.xml
```

### 14.2 前端项目结构
```
learning-recommend-frontend/
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── views/
│   │   ├── Login.vue
│   │   ├── Layout.vue
│   │   ├── student/
│   │   │   ├── Dashboard.vue
│   │   │   ├── MaterialSquare.vue
│   │   │   ├── MaterialDetail.vue
│   │   │   ├── Recommend.vue
│   │   │   ├── MyCollection.vue
│   │   │   └── Profile.vue
│   │   └── admin/
│   │       ├── MaterialManage.vue
│   │       └── UserManage.vue
│   ├── components/
│   │   ├── MaterialCard.vue
│   │   ├── RecommendList.vue
│   │   ├── ChartCard.vue
│   │   └── UploadFile.vue
│   ├── router/
│   │   └── index.js
│   ├── store/
│   │   ├── index.js
│   │   └── modules/
│   │       └── user.js
│   ├── api/
│   │   ├── user.js
│   │   ├── material.js
│   │   └── recommend.js
│   ├── utils/
│   │   ├── request.js
│   │   └── auth.js
│   └── assets/
├── public/
├── package.json
└── vite.config.js
```

### 14.3 AI服务项目结构
```
ai-service/
├── app/
│   ├── main.py
│   ├── models/
│   │   ├── summarizer.py
│   │   └── config.py
│   ├── api/
│   │   └── routes.py
│   └── utils/
│       └── text_processor.py
├── models/              # 模型文件(不提交git)
├── tests/
├── requirements.txt
├── config.yaml
└── README.md
```

---

## 十五、部署方案

### 15.1 开发环境

- **后端**: IDEA直接运行 (localhost:8080)
- **前端**: npm run dev (localhost:5173)
- **AI服务**: uvicorn main:app (localhost:5000)
- **数据库**: 本地MySQL + Redis

### 15.2 生产环境 (可选)

- **后端**: JAR包 + nohup
- **前端**: npm run build + Nginx
- **AI服务**: Docker容器
- **数据库**: 云服务器MySQL

---

## 十六、风险评估与应对

### 16.1 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| AI模型过大无法运行 | 中 | 高 | 使用轻量级模型/云API |
| 推荐效果不理想 | 中 | 中 | 调整参数/增加规则 |
| 前后端联调问题 | 低 | 中 | 提前定义接口规范 |
| 性能瓶颈 | 低 | 低 | Redis缓存优化 |

### 16.2 时间风险

- **预留缓冲**: 每个阶段预留20%时间
- **优先级**: 核心功能>美化>高级特性
- **最小化MVP**: 确保基础功能可用

---

## 十七、参考资料

### 17.1 技术文档

- [Spring Boot官方文档](https://spring.io/projects/spring-boot)
- [Vue 3官方文档](https://vuejs.org/)
- [Naive UI组件库](https://www.naiveui.com/)
- [MyBatis-Plus文档](https://baomidou.com/)
- [Hugging Face Transformers](https://huggingface.co/docs/transformers)

### 17.2 推荐算法

- 《推荐系统实践》- 项亮
- 《深度学习推荐系统》- 王喆
- Coursera: 推荐系统课程

### 17.3 Transformer模型

- "Attention is All You Need" 论文
- BART论文: "Denoising Sequence-to-Sequence Pre-training"
- T5论文: "Exploring the Limits of Transfer Learning"

---

## 十八、总结

### 核心优势
✅ 技术栈主流且成熟  
✅ 功能完整覆盖毕设要求  
✅ 推荐算法可解释性强  
✅ AI应用有实际价值  
✅ 演示效果直观

### 注意事项
⚠️ 控制Credit使用,优先解决核心问题  
⚠️ 数据准备充分,确保演示流畅  
⚠️ 代码规范,便于答辩时讲解  
⚠️ 备份模型/数据,避免意外

### 预期成果
🎯 完整可运行的系统  
🎯 清晰的技术文档  
🎯 可演示的推荐效果  
🎯 良好的答辩表现

---

**祝毕业设计顺利! 🎓**
