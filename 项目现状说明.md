## 项目现状说明

### 一、整体概览

- **项目定位**: 基于知识点的个性化学习推荐系统，集成学习资料管理、知识点管理、AI 问答与知识点生成、用户画像与智能推荐等功能，用于毕业设计展示与研究。
- **总体架构**: 前后端分离 + 独立 AI 服务：
  - **后端**: Spring Boot + MyBatis-Plus + MySQL + Redis，提供 REST API 和推荐算法逻辑。
  - **前端**: Vue 3 + Vite + Naive UI + Pinia + Vue Router，实现学生端与管理端界面。
  - **AI 服务**: 独立的 Python FastAPI 服务，负责摘要生成、关键词提取和简单问答/知识点生成，目前与 Java 后端尚未打通。
- **运行环境**:
  - MySQL 数据库 `learning_recommend`（见 `backend/src/main/resources/application.yml`）。
  - Redis 默认本地 `localhost:6379`，用于缓存/行为记录预留。
  - 通义千问 Qwen API 已在配置中预留（`ai.qwen.*`），由 Java 后端通过 `RestTemplate` 调用。

---

### 二、后端模块现状

#### 2.1 用户与认证模块

- **核心代码**:
  - 控制器: `UserController`、`AdminUserController`
  - 工具类: `JwtUtil`（负责生成/解析 JWT）
  - 实体与 Mapper: `User`、`UserProfile`、`UserMapper` 等
- **已实现功能**:
  - **学生端**:
    - 用户注册 `/user/register`、登录 `/user/login`（返回 JWT）。
    - 获取当前用户信息 `/user/info`，更新用户信息 `/user/info`（PUT）。
    - 获取用户统计 `/user/stats`，获取最近浏览记录 `/user/recent-views` 与知识点浏览记录 `/user/recent-knowledge-views`。
  - **管理员端**:
    - 用户列表分页查询 `/admin/user/list`，支持关键词与角色筛选。
    - 禁用/启用用户 `/admin/user/{id}/status`。
    - 删除用户 `/admin/user/{id}`。
    - 管理员编辑用户信息 `/admin/user/{id}`。
  - **认证与会话**:
    - 前端使用 Pinia `userStore` 保存 token 与用户信息，`request.js` 拦截器自动在请求头附加 `Authorization: Bearer <token>`。
    - 后端通过 `JwtUtil.getUserIdFromToken` 或静态方法 `JwtUtil.getUserId(HttpServletRequest)` 解析用户 ID。
- **未完全实现/差异点**:
  - 设计上有“权限控制”与“角色区分”需求，但当前代码层面主要依赖路径前缀（如 `/admin/**`）和前端路由 `meta.requiresAdmin` 控制，未看到基于 Spring Security 的细粒度权限配置。
  - 前端个人中心页面 `Profile.vue` 中“修改密码”功能仅前端提示“功能待实现”，后端没有对应修改密码接口。

#### 2.2 学习资料模块（资料广场 + AI 摘要展示）

- **核心代码**:
  - 控制器:
    - 学生端: `MaterialController`（路径 `/material/**`）
    - 管理端: `AdminMaterialController`（路径 `/admin/material/**`）
  - 服务与实现:
    - `MaterialService` + `MaterialServiceImpl`
  - 实体与 Mapper:
    - `LearningMaterial`（`t_material`）
    - `MaterialSummary`（`t_material_summary`）+ `MaterialSummaryMapper`
    - 行为与收藏: `UserBehavior`、`UserCollection` 及其 Mapper
- **学生端功能（已实现）**:
  - **资料广场**:
    - 列表查询 `/material/list`，支持关键词、分类、子分类、难度筛选，按创建时间、浏览量、评分排序。
    - 前端页面: `MaterialSquare.vue` 使用 `MaterialCard` 卡片展示。
  - **资料详情页** `/material/{id}`:
    - 展示标题、分类、难度、标签、浏览/收藏/评分等信息。
    - 描述内容支持 Markdown 渲染（`marked + highlight.js`）。
    - 若 `t_material_summary` 中存在对应记录，则展示:
      - `summaryText` 作为 AI 摘要。
      - `keyPoints`（JSON 数组）作为关键要点列表。
      - `keywords`（JSON 数组）作为关键词标签。
  - **行为记录与交互**:
    - 浏览记录 `/material/{id}/view`: 写入 `t_user_behavior`，累加 `viewCount`，并将浏览时长累计到 `UserProfile.learningDuration`。
    - 收藏/取消收藏 `/material/{id}/collect`: 更新 `t_user_collection`，增减 `collectCount`，记录行为类型 `collect/uncollect`。
    - 评分 `/material/{id}/rate`: 写入评分行为，重新计算资料的 `avgRating` 和 `ratingCount`。
    - 用户收藏列表 `/material/my-collections`。
- **管理员功能（已实现）**:
  - 资料列表（含多状态，在线/下架/待审核） `/admin/material/list`。
  - 上下架/审核 `/admin/material/{id}/status`、`/review`。
  - 编辑资料内容 `/admin/material/{id}`。
  - 删除/批量删除 `/admin/material/{id}`、`/batch`。
  - 资料统计 `/admin/material/stats`（总数/在线/待审核/下架/总浏览/总收藏）。
- **AI 摘要现状与差异**:
  - `MaterialServiceImpl` 只负责**读取** `MaterialSummary` 表中的摘要数据，没有任何代码调用 Python AI 服务或外部 LLM 来生成摘要。
  - `ai-service/main.py` 提供 `/api/summarize` 接口（简单 TextRank + jieba 实现），但 Java 后端与前端均未与之集成。
  - 设计文档中规划的“调用 FastAPI Transformer 模型生成摘要”的链路在当前代码中尚未打通，属于**手动/离线生成再入库**的模式。

#### 2.3 知识点模块（广场 + 详情 + 审核）

- **核心代码**:
  - 控制器:
    - 学生端: `KnowledgePointController`（路径 `/knowledge/**`）
    - 管理端: `AdminKnowledgePointController`（路径 `/admin/knowledge/**`）
  - 服务: `KnowledgePointService` + `KnowledgePointServiceImpl`
  - 实体: `KnowledgePoint`（`t_knowledge_point`）、`UserBehavior`、`UserCollection`
- **学生端功能（已实现）**:
  - **知识点广场** `/knowledge/list`:
    - 支持关键词检索（标题/描述/内容）、分类、子分类、难度筛选，按创建时间等排序。
    - 前端页面: `KnowledgeSquare.vue`，使用 `KnowledgePointCard` 展示。
  - **知识点详情** `/knowledge/{id}`:
    - 展示分类、难度、标签、浏览数、收藏数、平均评分等。
    - 使用 Markdown 渲染 `content` 字段，支持代码高亮。
    - 支持收藏/取消收藏、评分，前端实时更新。
  - **行为记录**:
    - 浏览 `/knowledge/{id}/view`，写入 `t_user_behavior`，更新 `viewCount`，并通过 `UserProfileService.recordKnowledgeView` 更新画像。
    - 收藏/取消收藏 `/knowledge/{id}/collect`，写入行为 `collect/uncollect`，更新 `collectCount`，并通过 `recordKnowledgeCollect` 更新画像。
    - 评分 `/knowledge/{id}/rate`，记录评分行为并重新计算平均评分和评分人数。
  - **我的收藏** `/knowledge/my-collections`，分页返回知识点收藏列表。
  - **用户贡献知识点** `/knowledge/contribute`:
    - 学生可以提交新知识点（标题、分类、标签、内容等），后端存为 `status=2`（待审核），等待管理员审核。
- **管理员功能（已实现）**:
  - 知识点列表 `/admin/knowledge/list`，包含所有状态（上架/下架/待审核）。
  - 审核知识点 `/admin/knowledge/{id}/audit`（通过/拒绝 + 原因）。
  - 上下架 `/admin/knowledge/{id}/status`，以及批量上架/下架 `/admin/knowledge/batch/status`。
  - 删除/批量删除 `/admin/knowledge/{id}`、`/batch`。
  - 编辑知识点内容 `/admin/knowledge/{id}`。
  - 统计接口 `/admin/knowledge/stats`。
- **与设计文档对比**:
  - 文档中“知识点基础设施已完成”的部分在代码中已经完全落地：实体、Mapper、Service、Controller、前后端页面、收藏/评分、用户贡献与审核逻辑均已实现。

#### 2.4 问答模块与 AI 知识点生成

- **核心代码**:
  - 控制器: `QAController`（路径 `/api/qa/**`）
  - 服务: `QAService` + `QAServiceImpl`
  - 实体/Mapper: `QARecord`（`t_qa_record`）+ `QARecordMapper`
  - AI 服务接口: `AIService` + `AIServiceImpl`
- **后端功能实现**:
  - **提问接口** `/api/qa/ask`:
    - 从请求中解析 `userId`，调用 `AIService.answerQuestion(question)` 得到文本回答（Markdown 风格内容）。
    - 将问答记录写入 `t_qa_record`，包括问题、答案、模型名称、耗时。
    - 若注入了 `UserProfileService`，则调用 `recordQABehavior(userId, question, category)`，基于简单关键词匹配推断问题类别并更新用户画像（问答主题分布与分类偏好）。
  - **从问答生成知识点** `/api/qa/generate/{qaId}`:
    - 根据 `qaId` 查询 `QARecord`，校验记录存在且属于当前用户。
    - 调用 `AIService.generateKnowledgePoint(question, answer)` 获得结构化知识点数据（标题、分类、子分类、描述、标签、难度）。
    - 以“待审核”状态向 `t_knowledge_point` 新增一条记录，并返回新知识点 ID。
  - **问答历史** `/api/qa/history`:
    - 分页返回当前用户问答记录列表 `QARecordVO`，若问答记录关联了生成的知识点，会额外显示该知识点标题。
- **AIService 实际行为**:
  - 若 `ai.qwen.enabled=false` 或未配置有效 `api-key`，则所有 AI 能力采用本地“占位实现”：
    - `answerQuestion`: 返回基于问题的固定模板回答（非真正大模型）
    - `generateKnowledgePoint`: 按简单逻辑构造知识点（截取问题/答案 + 默认分类与标签）
    - `batchGenerateKnowledgePoints`: 返回若干示例知识点
  - 若 `ai.qwen.enabled=true` 且配置了 `api-key`，则通过 `RestTemplate` 调用通义千问的 text-generation 接口，按复杂 Prompt 生成 Markdown 知识点或 JSON 结构，并解析响应，作为真实 AI 输出。
- **前端问答页面**:
  - 页面: `QA.vue`
  - 已实现功能:
    - 提问输入、提交与加载状态展示。
    - 显示当前回答（模型名称 + 耗时 + 文本）。
    - 问答历史列表，支持查看历史答案、从历史记录一键“生成知识点”。
    - “一键生成知识点”时弹出编辑弹窗，预填 AI 返回的知识点结构，用户可编辑后调用 `/knowledge/contribute` 提交审核。
- **与文档对比**:
  - 文档中“AI 问答模块已完成”的说法与代码基本一致：后端问答接口、问答记录存储、前端页面和知识点生成流程均已实现。
  - 真正是否调用在线大模型取决于运行时配置；若关闭 `ai.qwen.enabled` 则退化为本地占位逻辑。

#### 2.5 用户画像与推荐模块

- **用户画像 (`UserProfile`)**:
  - 实体: `UserProfile`（`t_user_profile`）
  - 服务: `UserProfileService` + `UserProfileServiceImpl`
  - 功能:
    - 自动初始化画像（学习水平、兴趣标签、问答主题分布等默认值）。
    - 异步更新画像：统计知识点浏览/收藏总数，分析知识点分类偏好，提取兴趣标签，计算学习水平（初/中/高）。
    - 记录行为：通过 `recordKnowledgeView`、`recordKnowledgeCollect`、`recordQABehavior` 实时更新计数和偏好分布。
    - 提供接口:
      - `/api/user-profile`: 获取完整画像。
      - `/api/user-profile/stats`: 获取学习统计指标。
      - `/api/user-profile/preferences`: 获取兴趣标签、问答主题分布与知识点偏好。
      - `/api/user-profile/refresh`: 手动触发画像重新计算（异步）。
  - 前端页面: `UserProfile.vue`，展示统计数字、偏好进度条、问答主题分布和最喜欢的分类，并支持“一键刷新画像”。
- **资料推荐 (`RecommendService`)**:
  - 接口: `RecommendService` + `RecommendServiceImpl`
  - 控制器: `RecommendController`（路径 `/recommend/**`）
  - 实现逻辑:
    - 协同过滤推荐：基于 `UserBehavior` 的浏览/收藏记录，寻找相似用户并推荐其喜欢但当前用户未浏览的资料。
    - 基于内容的推荐：根据用户画像中的兴趣标签，与资料标签匹配计算得分。
    - 热门推荐：基于 `viewCount` 和 `status` 从 `t_material` 中选择热门资料。
  - 暴露接口:
    - `/recommend/personalized`: 个性化资料推荐。
    - `/recommend/hot`: 热门资料推荐。
    - `/recommend/similar/{materialId}`: 相似资料推荐（同分类 + 评分排序）。
  - 前端页面: `Recommend.vue` 中的“学习资料” Tab 使用上述接口，并支持评分/收藏等交互。
- **知识点每日推荐与 AI 创作 (`DailyRecommendService`)**:
  - 接口: `DailyRecommendService` + `DailyRecommendServiceImpl`
  - 控制器: `DailyRecommendController`（路径 `/api/daily/**`）
  - 实现逻辑:
    - `getDailyRecommendations(userId, count)`: 按用户画像返回每日推荐知识点列表：
      - 若无画像，回退到热门知识点。
      - 否则按 Favorite Category、知识点偏好和多样性策略组合推荐。
    - `generateRecommendedKnowledgePoint(userId, category)`: 基于用户画像和指定分类构造 Prompt，通过 `AIService.generateKnowledgePoint` 生成知识点，并直接上架（`status=1`，系统生成）。
  - 前端页面: `DailyRecommend.vue`
    - 实现了一个“AI 知识点创作”流程：展示用户画像概览 -> 选择主题 -> 调用问答/AI 生成知识点草稿 -> 编辑后提交审核。
    - 实际生成知识点调用的是后端 `QA`+`KnowledgePoint`+`AIService` 链路，而非 Python AI 服务。
- **与文档的差异**:
  - 文档中推荐算法部分（协同过滤 + 内容推荐 + 混合策略）在资料推荐侧已有近似实现。
  - 知识点推荐部分（`RecommendService` 针对知识点）在代码中由 `DailyRecommendServiceImpl` 实现，逻辑与文档描述一致，但接口路径与命名略有不同。
  - 文档提到的“定时任务离线计算 + Redis 缓存”在当前代码中尚未看到定时任务与缓存逻辑，多数推荐是在请求时即时计算完成。

#### 2.6 资料采集与管理后台

- **资料采集 (`CrawlerController` + `CrawlerService`)**:
  - 控制器: `/admin/crawler/**`
  - 前端页面: `MaterialCrawler.vue`，支持：
    - 按关键词与来源搜索外部资料。
    - 在表格中选择若干记录，批量导入到系统。
  - 具体爬虫实现类位于 `crawler.impl` 下，代码结构完整。
- **管理后台其他模块**:
  - `UserManage.vue`: 用户管理，调用 `/admin/user/**`。
  - `KnowledgePointManage.vue`: 知识点管理，调用 `/admin/knowledge/**`。
  - `MaterialManage.vue`: 资料管理，调用 `/admin/material/**`。
  - `Statistics.vue`: 资料统计与快速导航。

---

### 三、前端路由与页面结构现状

- **路由结构**（`frontend/src/router/index.js`）：
  - 根布局 `/`，重定向到 `/dashboard`，子路由包含：
    - `/dashboard`: 首页（包含统计与推荐，代码在 `Dashboard.vue`）。
    - `/knowledge`: 知识点广场。
    - `/knowledge/:id`: 知识点详情。
    - `/qa`: AI 问答。
    - `/material`: 资料广场。
    - `/material/:id`: 资料详情。
    - `/recommend`: 智能推荐页面（知识点 + 资料）。
    - `/daily`: 每日推荐 / AI 知识点创作。
    - `/collection`: 我的收藏。
    - `/profile`: 个人中心（基础信息与兴趣标签管理）。
    - `/user-profile`: 我的画像。
  - 管理后台 `/admin` 下：
    - `/admin/knowledge`: 知识点管理。
    - `/admin/material`: 资料管理。
    - `/admin/crawler`: 资料采集。
    - `/admin/user`: 用户管理。
    - `/admin/stats`: 统计。
- **状态管理**:
  - `store/user.js` 使用 Pinia 保存 token 和用户基础信息，提供 `setToken`、`setUserInfo` 和 `logout` 方法，配合 Axios 拦截器实现登录态维护与 401 时跳转登录。

---

### 四、AI 服务（Python）现状

- **代码位置**: `ai-service/main.py`
- **已实现接口**:
  - `GET /`: 健康状态简要检查。
  - `GET /health`: 返回模型加载状态、GPU 可用性等占位信息（当前均为 False）。
  - `POST /api/summarize`: 接收长文本，使用简单分句 + jieba 提取关键词 + 前几句作为“摘要”的临时实现，并返回 `summary`、`key_points`、`keywords`、`model`、耗时等。
  - `POST /api/keywords`: 提取关键词及权重。
  - `POST /api/qa/answer`: 简单规则问答，占位实现。
  - `POST /api/qa/generate-knowledge`: 基于问题+回答提取关键词，生成简单的知识点结构（标题、分类、子分类、描述、标签、难度）。
- **依赖**: `fastapi`, `uvicorn`, `transformers`, `torch`, `jieba` 等已在 `requirements.txt` 中声明，但当前代码并未真正加载 Transformer 模型，仍是占位级实现。
- **与主系统集成情况**:
  - Java 后端代码和前端 API 中未调用该 FastAPI 服务。
  - 设计方案中的 `AI_SERVICE_URL = "http://localhost:5000/api/summarize"` 仅出现在文档中，未在实际 Java 代码中使用。
  - 当前系统的 AI 逻辑主要通过 Java 侧 `AIServiceImpl` 调用通义千问，而非调用本地 `ai-service`。

---

### 五、与设计文档的总体差异总结

- **已完成且代码实现与文档基本一致的部分**:
  - 知识点基础设施：实体、CRUD、审核、前后端页面、学生端使用流程。
  - AI 问答模块：问答接口、问答记录、从问答生成知识点、前端问答页面与知识点编辑弹窗。
  - 用户画像模块：行为数据写入、画像字段、异步更新、前端画像展示。
  - 资料管理模块：资料 CRUD、审核、统计、学生端浏览/收藏/评分与详情页展示。
  - 管理后台：用户、资料、知识点、采集、统计等功能页面与对应接口。

- **部分实现或存在差异的功能**:
  - 推荐系统:
    - 资料推荐（协同过滤 + 标签内容匹配 + 热门补充）已在 `RecommendServiceImpl` 中实现。
    - 知识点推荐偏向通过 `DailyRecommendServiceImpl` + 画像偏好实现，而非单独的通用 `RecommendService` 方法。
    - 尚未看到定时任务+Redis 缓存形式的“离线推荐计算”，当前多为实时计算。
  - AI 模型调用:
    - 文档中规划的是 Python FastAPI + Transformer 模型作为 AI 核心。
    - 实际落地中，Java 侧通过 `AIServiceImpl` 调用通义千问 HTTP API；Python 服务目前处于“可运行但未集成”的占位状态。
    - AI 问答和知识点生成的实际“智能程度”取决于是否正确配置并启用通义千问；否则以规则/示例数据兜底。
  - 资料摘要:
    - 展示逻辑完整（从 `t_material_summary` 读取），但摘要生成流程未集成 AI 服务，需要通过单独任务/脚本预先写入摘要表。
  - 安全与权限:
    - 文档中提到 JWT + 角色控制，代码层面 JWT 已生效，但未引入 Spring Security 的权限注解或拦截器，管理员权限主要由路由与接口前缀约定实现。
  - 一些前端提示功能尚未有后端支撑（如个人中心的“修改密码”）。

---

### 六、当前可演示的核心能力小结

综合以上代码分析，当前项目在不调整配置的情况下，具备以下**可实际演示**的核心功能：

1. **用户体系**: 注册、登录、退出、个人信息编辑（头像、昵称、邮箱、兴趣标签）、基本统计和浏览记录。
2. **学习资料**: 资料广场（搜索/筛选/排序）、资料详情（Markdown 描述 + AI 摘要展示）、浏览/收藏/评分、我的收藏、管理员资料管理与统计。
3. **知识点体系**: 知识点广场与详情（Markdown 内容）、浏览/收藏/评分、学生贡献知识点、管理员审核与上下架、知识点统计。
4. **AI 问答与知识点生成**: 问答交互、问答历史、从问答一键生成知识点草稿并提交审核；若配置了通义千问，可获得真实大模型输出。
5. **用户画像**: 基于浏览/收藏/问答行为实时更新的画像字段，前端画像页展示学习统计和偏好分布，并支持手动刷新。
6. **推荐与每日推荐**: 
   - 资料层面的个性化推荐与热门推荐（`/recommend/**`）。
   - 知识点层面的每日推荐与 AI 创作流程（`/api/daily/**` + `DailyRecommend.vue`），基于用户画像生成主题和知识点。
7. **管理后台**: 用户管理、资料管理、知识点管理、资料采集和整体统计页面，形成完整的运营后台闭环。

若后续要继续完善，建议优先方向包括：
- 将 Python `ai-service` 与后端资料摘要/知识点生成流程真正打通，或统一改为只使用通义千问/其他大模型，以减少重复。
- 为推荐结果增加 Redis 缓存和定时更新任务，贴近文档描述的“离线计算 + 在线读取”架构。
- 引入 Spring Security 角色权限控制，强化管理员接口的访问安全。
- 为“修改密码”等前端预留入口补齐后端实现。

### 七、近期改动记录

- **2025-12-26 14:00**：修正用户画像分类偏好统计（不再全部归为"算法"），基于知识点真实分类计算；注册与个人中心根据兴趣标签初始化 `favoriteCategory` 和 `knowledgePreferences`，并调整每日推荐默认主题使用真实偏好。
- **2025-12-26 14:30**：扩展注册/个人中心兴趣标签选项（增加数据结构、操作系统、计算机网络、机器学习、深度学习、移动开发、大数据、云原生、软件工程等），并区分 AI 问答历史与 AI 知识点创作记录，使问答历史只展示用户真实提问。
- **2025-12-26 16:20**：集成 Transformer 模型到 Python AI 服务（`ai-service/main.py`），使用 `transformers` 库懒加载中文 T5 模型，优先使用模型生成摘要，失败时回退到简单算法，实现"基于 Transformer 的大模型"主要目标。
- **2025-12-26 16:40**：优化问答历史显示逻辑（生成主题不显示，生成知识点内容显示）与简化 AI 提示词（前后端均去掉过度修饰，改为正常人视角提问方式，字数要求从 500+ 降至 300-800，知识点主题生成提示词也简化为更直接的格式）。
- **2025-12-29 16:50**：优化登录/注册界面文案与视觉效果，将标题调整为“AI 学习推荐平台”，增加大模型特色亮点标签，并改为卡片顶部渐变 Header + 底部表单区的布局，整体观感更贴近正式产品登录页。